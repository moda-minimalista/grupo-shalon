rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Storage está desativado neste projeto. Ative-o antes de alterar esta regra.
    match /{allPaths=**} { allow read, write: if false; }
  }
}
